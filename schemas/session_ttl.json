{
  "type": "object",
  "properties": {
    "ttl_seconds": {
      "type": "integer",
      "minimum": 1,
      "maximum": 86400
    },
    "tier": {
      "enum": ["free", "pro", "enterprise"]
    }
  },
  "required": ["ttl_seconds", "tier"]
}
